# 개발 이력

## 2026-04-09 (초기 개발)

### Phase 1: 프로젝트 초기화
- `package.json` 생성 (dreamitbiz-hrm, React 19, Vite 7, TypeScript, Supabase)
- `vite.config.ts` 생성 (포트 5190, base: /Research/)
- `tsconfig.json` 생성 (strict mode, ES2020 target)
- `index.html` 생성 (HRM 메타데이터, 파란색 테마)
- `.gitignore`, `.env.example` 생성
- `public/404.html`, `CNAME`, `robots.txt`, `sitemap.xml` 생성

### Phase 2: 타입 및 설정
- `src/types/index.ts` - 전체 타입 정의 (Language, Theme, LearningPath, Product, Order, User, Comment, Toast, ColorTheme)
- `src/config/site.ts` - 사이트 설정 + 8개 HRM 학습 경로 정의
- `src/config/admin.ts` - 관리자 이메일 목록

### Phase 3: 핵심 인프라 (Contexts, Utils, Hooks)
- **Contexts**: ThemeContext, LanguageContext, AuthContext, ToastContext
  - ThemeContext: 기본 컬러 테마를 'hrm' (파란색)으로 설정
  - AuthContext: Supabase OAuth + 프로필 + 관리자 감지
- **Utils**: supabase.ts, auth.ts, storage.ts, translations.ts
  - supabase.ts: hrm_ 접두사 테이블 사용
  - translations.ts: HRM 관련 한/영 번역
- **Hooks**: useAOS.ts (스크롤 애니메이션), useCountUp.ts (숫자 카운터)

### Phase 4: 컴포넌트
- **공통**: SEOHead, TipBox, FeatureCard, HeroBackground, HeroCarousel, CodeBlock
- **레이아웃**: Navbar (8개 HRM 메뉴), Footer (3열)
- **라우터**: PublicLayout (React Router v7, lazy loading)

### Phase 5: CSS 스타일시트 (13개)
- study 사이트에서 13개 CSS 파일 복사
- `#059669` (초록) → `#2563EB` (파란)으로 전체 색상 교체
- 파일: base, navbar, hero, home, guide-pages, code-blocks, footer, community, auth, toast, dark-mode, animations, responsive

### Phase 6: 페이지 개발
- **일반 페이지**: Home, NotFound, Login, Register, ForgotPassword, MyPage
- **HRM 학습 페이지 (8개)** - 각 페이지 6개 섹션 + 학술 참고문헌 포함:

| 페이지 | 출처 수 | 주요 참고 학자/문헌 |
|--------|---------|---------------------|
| HRM 개론 | 11 | Taylor(1911), Mayo(1933), Ulrich(1997), Barney(1991) |
| 채용/선발 | 8 | Schmidt&Hunter(1998), Bauer(2010), Breaugh(2008) |
| 교육/개발 | 8 | Kirkpatrick(2006), Knowles(2015), Kolb(1984), Noe(2020) |
| 성과관리 | 8 | Aguinis(2019), Kaplan&Norton(1996), Locke&Latham(1990) |
| 보상관리 | 8 | Adams(1963), Milkovich(2020), Lawler(1990), Kerr(1975) |
| 노사관계 | 8 | Dunlop(1958), Freeman&Medoff(1984), Kochan(1986) |
| 조직문화/리더십 | 9 | Schein(2010), Cameron&Quinn(2011), Kotter(1996), Bass(2006) |
| HR 트렌드 | 8 | Schwab(2016), Frey&Osborne(2017), Davenport(2010), WEF(2023) |

### Phase 7: 빌드 & 배포
- `npm install` - 136 패키지, 0 취약점
- `npx tsc --noEmit` - TypeScript 0 errors
- `npm run build` - Vite 빌드 성공 (3.48초)
- `npx gh-pages -d dist` - hrm.dreamitbiz.com 배포

---

## 2026-04-09 (리포지토리 분리 & UI 수정)

### Phase 8: research → hrm 리포지토리 분리
- **문제**: HRM 사이트가 `aebonlee/Research` 리포에 배포되어 기존 조사방법론 사이트 덮어씀
- **해결**: 별도 `aebonlee/hrm` 리포 생성, 전체 참조 변경
  - `CNAME`: research.dreamitbiz.com → hrm.dreamitbiz.com
  - `package.json` name: dreamitbiz-research → dreamitbiz-hrm
  - `site.ts`: id/name/url/github/dbPrefix 전체 변경 (hrm_, aebonlee/hrm)
  - `sitemap.xml`, `robots.txt`, `index.html` OG 태그 일괄 변경
  - `translations.ts`: "DreamIT Research" → "DreamIT HRM"
  - Dev_md 문서 일괄 갱신
- `aebonlee/Research` 리포를 조사방법론 마지막 커밋(368ede5)으로 force push 복원
- 양 사이트 모두 gh-pages 재배포 완료

### Phase 9: OG 이미지 & 파비콘 교체
- `og-image.png`: "DreamIT HRM / 인적자원관리(HRM)" + 8개 학습 경로 뱃지 (sharp + SVG → PNG)
- 파비콘: HRM 로고 (HR/M 파란 그라데이션) SVG + PNG 4사이즈 생성
- `og:site_name`: "DreamIT Biz" → "DreamIT HRM"
- 이미지 생성 스크립트 `scripts/generate-og.mjs`, `scripts/generate-favicons.mjs` 추가

### Phase 10: 푸터 & 번역 수정
- Footer 빠른 링크: 1열 → **2열 그리드** 배치 (`.footer-link-grid` CSS 추가)
- Footer 브랜드명: "HRM Research" → "DreamIT HRM"
- `translations.ts` features 키 불일치 수정:
  - `habits` → `recruit` (인재 확보 / Talent Acquisition)
  - `memory` → `develop` (역량 개발 / Competency Dev)
  - `strategy` → `perform` (성과 관리 / Performance Mgmt)

### Git 이력
| 커밋 | 설명 |
|------|------|
| `72b731c` | refactor: research → hrm 분리 |
| `c0b6090` | fix: OG 이미지/파비콘 교체, 푸터 2열, 번역키 수정 |

---

## 기술적 특이사항
- study 사이트 아키텍처를 100% 동일하게 유지하여 코드 일관성 확보
- 모든 학습 페이지에 학술적 참고문헌(References) 섹션 포함
- 인라인 인용(학자명, 연도)과 통합 참고문헌 목록 병행
- 한/영 바이링구얼 컨텐츠 완전 지원
